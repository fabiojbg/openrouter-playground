services:
  openrouter-playground:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=3002
